#version 330

in vec2 UV;
out vec4 outColor;
uniform sampler2D texFramebuffer;

void main()
{
    vec2 uv = UV;
    uv *= 1.0 - uv.yx;
    float vig = uv.x*uv.y* 80.0;
    vig = pow(vig, 0.8);

    vec4 vignetteCol = texture(texFramebuffer, UV)*vig;

    gl_FragColor = vignetteCol;
}